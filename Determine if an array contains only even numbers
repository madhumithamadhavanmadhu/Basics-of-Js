function areAllEven(arr) {
    return arr.every(function(number) {
        return number % 2 === 0;
    });
}
const numbers = [2, 4, 6, 8, 10];
const allEven = areAllEven(numbers);
console.log(allEven); 
