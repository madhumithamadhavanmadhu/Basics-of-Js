const arr = [10,20,30.40,50,60];
const divisor = 50;
let count = 0;
for (let i = 0; i < arr.length; i++) {
    if (arr[i] % divisor === 0) {
        count++;
    }
}
console.log("Number of elements divisible by", divisor, "is:", count);
