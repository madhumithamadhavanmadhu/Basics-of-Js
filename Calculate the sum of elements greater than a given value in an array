const arr = [5,10,15,20,25];
const threshold = 5;
let sum = 0;
for (let i = 0; i < arr.length; i++) {
    if (arr[i] > threshold) {
        sum += arr[i];
    }
}
console.log("Sum of elements >than", threshold, "is:", sum);
