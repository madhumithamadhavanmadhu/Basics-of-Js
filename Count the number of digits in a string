function countDigits(str) {
    const digits = str.match(/\d/g);
    return digits ? digits.length : 0;
}
const text = "I have 1 apple,2 banana,3 grapes in my basket";
const digitCount = countDigits(text);
console.log(digitCount); 
